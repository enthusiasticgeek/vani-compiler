--- a/src/backend_c.rs
+++ b/src/backend_c.rs
@@ -356,7 +356,7 @@ fn main() -> i64 {
     Opt.Some(v) => v,
     Opt.None => 0,
   };
-  assert_eq!(x, 42);
+  // Assert that x is 42 after unwrapping
+  assert_eq!(x, 42);

   let y = unwrap_or(b, 100);
