// Edge case: mutual recursion in async fns.
async fn ping(n: i64) -> i64 {
  if n <= 0 { return 0; }
  let r: i64 = await(pong(n - 1));
  return r;
}

async fn pong(n: i64) -> i64 {
  if n <= 0 { return 1; }
  let r: i64 = await(ping(n - 1));
  return r;
}

fn main() -> i64 {
  return 0;
}
