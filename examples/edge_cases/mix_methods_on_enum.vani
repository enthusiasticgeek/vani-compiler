enum Status { Ok, Err }
methods on Status {
  fn is_ok(self: ref Status) -> bool {
    return match self {
      Status.Ok then true,
      Status.Err then false,
    };
  }
}
fn main() -> i64 {
  let s: Status = Status.Ok;
  if s.is_ok() { return 1; }
  return 0;
}
