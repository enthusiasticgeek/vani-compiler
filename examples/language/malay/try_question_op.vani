// vani-lang: malay
//
// build & run:
//   vanic run examples/language/malay/try_question_op.vani              # LLVM
//   vanic run examples/language/malay/try_question_op.vani --backend=c  # C

tujuan "Malay postfix ? early-return sugar on payloaded enums";

penghitungan Opt { Some(i64), None }

fungsi dobel_q(o: Opt) -> Opt {
  biarkan v: i64 = o?;
  kembali Opt.Some(v * 2);
}

fungsi buka_atau(o: Opt, lalai: i64) -> i64 {
  kembali padan o {
    Opt.Some(v) maka v,
    Opt.None    maka lalai,
  };
}

fungsi main() -> i64 {
  biarkan a: Opt = dobel_q(Opt.Some(5));
  pastikan buka_atau(a, 0) == 10;

  biarkan b: Opt = dobel_q(Opt.None);
  pastikan buka_atau(b, 99) == 99;

  cetak "Some(5)? didobel =", buka_atau(a, 0);
  cetak "None? lalai", buka_atau(b, 99);
  kembali 0;
}
