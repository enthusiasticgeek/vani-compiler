// vani-lang: greek
//
// build & run:
//   vanic run examples/language/greek/iterate.vani              # LLVM
//   vanic run examples/language/greek/iterate.vani --backend=c  # C

σκοπός "Greek vec iteration";

συνάρτηση vec_sum(v: Vec<i64>) -> i64 {
  έστω total: i64 = 0;
  για i από 0 μέχρι len(v) {
    total = total + v[i];
  }
  επιστροφή total;
}

συνάρτηση vec_max(v: Vec<i64>) -> i64 {
  έστω m: i64 = v[0];
  για i από 1 μέχρι len(v) {
    αν v[i] > m {
      m = v[i];
    }
  }
  επιστροφή m;
}

συνάρτηση main() -> i64 {
  έστω v: Vec<i64> = vec(3, 1, 4, 1, 5, 9);
  επιβεβαίωση vec_sum(v) == 23;
  επιβεβαίωση vec_max(v) == 9;
  εκτύπωση "sum =", vec_sum(v);
  εκτύπωση "max =", vec_max(v);
  επιστροφή 0;
}
