# vāṇī v0.1.7 — Tutorial coverage expansion (2026-06-21)

This is a documentation / tutorial release. No compiler changes; all
new content is in `tutorials/src/`. The book now covers every shipped
built-in and language feature through v0.1.6.

---

## New tutorial pages (7 pages, ~1 650 lines)

### Intermediate

| Page | Content |
|------|---------|
| `intermediate/06c_fnptr_primer.md` | **Function pointers as first-class values** — `fn(A)->R` type syntax, higher-order functions, anonymous `fn` literals at call sites, returning `fn` from `fn`, `Vec<fn(A)->R>` dispatch tables, closures-vs-fnptr comparison |
| `intermediate/09b_file_io_primer.md` | **Native file I/O primer** — `FileHandle` affine RAII concept, 9 builtins API table, `eprint` / `stdin` mental model, FFI boundary |
| `intermediate/09c_file_io.md` | **Native file I/O worked examples** — write/read file, stdin prompt, append mode, line-counting challenge |
| `intermediate/15a_math_deep.md` | **Math library deep-dive** — logs (`log2`, `log10`, `f64_log_b`, `f64_expm1`, `f64_log1p`, `f64_exp2`), special functions (`f64_erf`, `f64_erfc`, `f64_tgamma`, `f64_lgamma`, `f64_hypot`, `f64_cbrt`), ML activations (`f64_relu`, `f64_sigmoid`, `f64_swish`, `f64_softplus`, `f64_softsign`, `f64_leaky_relu`, `f64_logit`), extended number theory (`i64_mod_pos`, `i64_mod_inverse`, `i64_totient`, `i64_radical`, `f64_quadratic_root`), full bit-manipulation API (`i64_set_bit`, `i64_clear_bit`, `i64_toggle_bit`, `i64_test_bit`, `i64_parity`, `i64_leading_zeros`, `i64_trailing_zeros`, `i64_reverse_bits`, `i64_rotate_left`, `i64_rotate_right`, `i64_bswap`) |
| `intermediate/15b_vec_stats.md` | **Vec statistics and combinators** — sorting variants, argmin/argmax/kth/median, statistical aggregates, running/cumulative ops, set operations, functional and mutation combinators |

### Advanced

| Page | Content |
|------|---------|
| `advanced/03b_condvar_primer.md` | **Condition variables** — wait-loop idiom, spurious wakeups, `Condvar` + `Mutex<T>` + `Guard<T>` API, `wait_timeout`, unlock-before-notify, when not to use condvar |
| `advanced/04b_cross_compile_primer.md` | **Cross-compilation primer** — target triple system, `--no-std`, `#[no_mangle]`, `#[link_section]`, all 6 MMIO widths, cross-linker, QEMU user-mode run |
| `advanced/04c_attributes_reference.md` | **Function attributes reference** — all 8 attributes with examples and runtime-cost table: `#[no_heap]`, `#[bounded_stack(N)]`, `#[recursion_bound(N)]`, `#[deterministic_timing]`, `#[interrupt]`, `#[bounded(N)]`, `#[no_mangle]`, `#[link_section = "..."]` |
| `advanced/05b_advanced_collections.md` | **Advanced collections** — Graph, BST/AVL, Trie, SkipList, UnionFind, BloomFilter, Deque — full API tables and decision guide |

### Beginner

| Page | Content |
|------|---------|
| `beginner/00_cli_reference.md` | **`vanic` CLI reference** — all subcommands (`run`, `build`, `check`, `emit`, `fmt`, `test`, `tokens`, `ast`, `ir`, `stack-depth`, `acyclicity`, `deviations`, `audit-pack`, `complexity`, `safety-attrs`, all Kosh subcommands), all flags, all environment variables |

---

## What has not changed

- Compiler: identical to v0.1.6
- Language semantics: no changes
- Built-ins: no additions (all documented builtins were already implemented)
- ABI / IR: no changes
- Test count: unchanged (~2434+ lib green)

---

## Upgrade

No code changes required. Update your local clone:

```bash
git pull
```

The online tutorial book at
<https://enthusiasticgeek.github.io/vani-compiler/> rebuilds automatically
from the `main` branch via GitHub Actions.
